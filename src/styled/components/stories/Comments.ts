import styled from "styled-components";

export const CommentsCountLabel = styled.p`
  text-align: left;
`;

export const CommentContent = styled.li`
  margin: 0;
  padding: 0;
  text-align: left;
  list-style: none;
  
  p {
    margin: 0 0 0 1rem;
    padding: 0;
  }
  
  h5 {
    margin-bottom: .25rem;
  }
`;

export const CommentList = styled.ul`
  list-style: none;
  padding-left: 0;
  margin-top: 0;
`
